# Responsividad Mobile — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que toda la app navegue bien en celular: layouts que apilan, tipografía fluida, tap targets cómodos y un carrito como barra fija inferior en "Arma tu mesa".

**Architecture:** El layout vive en estilos inline (no soportan media queries). Introducimos un set chico de **clases de layout responsivas** en `globals.css` con un breakpoint `@media (max-width: 640px)`, y reemplazamos los grids/filas inline problemáticos por esas clases. Tipografía grande con `clamp()` (sí funciona inline). Sin dependencias nuevas en runtime. Verificación visual con Playwright a 390px (usuario de prueba para pantallas con sesión).

**Tech Stack:** Next.js 16 (App Router), CSS en `globals.css`, Playwright (solo devDependency para capturas).

**Branch:** `feat/mobile-ux` (crear desde `main`).

**Nota:** Es una pasada de estilos/layout, sin lógica nueva → no hay pruebas unitarias. La verificación de cada tarea es `pnpm exec tsc --noEmit` (sin errores) + revisión visual de capturas (Tarea 7).

---

## Estructura de archivos

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `src/app/globals.css` | Clases de layout responsivas + media query móvil + barra inferior | Modificar |
| `src/app/dashboard/page.tsx` | Grid del panel → clase `.grid-panel` | Modificar |
| `src/app/dashboard/mesa/[slug]/page.tsx` | Grid → `.grid-mesa`, aside → `.mesa-aside`, barra inferior móvil | Modificar |
| `src/app/dashboard/mesa/[slug]/CatalogoSelector.tsx` | Filas → `.fila-catalogo` / `.fila-catalogo-form` | Modificar |
| `src/app/page.tsx` | Hero con `clamp()` | Modificar |
| `scripts/seed-usuario-prueba.mjs` | Crea usuario de prueba + mesa con ítems (solo dev/verificación) | Crear |
| `scripts/capturas-movil.mjs` | Playwright: login + capturas a 390px | Crear |

---

## Task 1: Clases responsivas en `globals.css`

**Files:**
- Modify: `src/app/globals.css`

- [ ] **Step 1: Agregar las clases responsivas al final del archivo**

Añadir al final de `src/app/globals.css`:

```css
/* ===== Layouts responsivos ===== */
.grid-panel {
  display: grid;
  grid-template-columns: minmax(280px, 360px) 1fr;
  gap: 1.5rem;
  align-items: start;
}
.grid-mesa {
  display: grid;
  grid-template-columns: 1fr minmax(280px, 340px);
  gap: 1.75rem;
  align-items: start;
}
.mesa-aside {
  position: sticky;
  top: 1.5rem;
}

/* Filas del catálogo (imagen + info + controles) */
.fila-catalogo {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 0.6rem 0.85rem;
}
.fila-catalogo-form {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

/* Barra de resumen móvil: oculta en desktop */
.barra-movil {
  display: none;
}

@media (max-width: 640px) {
  /* Layouts apilan en una columna */
  .grid-panel,
  .grid-mesa {
    grid-template-columns: 1fr;
  }
  .mesa-aside {
    position: static;
  }

  /* Filas del catálogo se reacomodan vertical */
  .fila-catalogo {
    flex-wrap: wrap;
  }
  .fila-catalogo-form {
    flex-basis: 100%;
    flex-wrap: wrap;
    justify-content: space-between;
  }

  /* Tap targets más cómodos */
  .btn {
    padding: 0.8rem 1.1rem;
  }

  /* Barra de resumen fija abajo */
  .barra-movil {
    display: flex;
    position: fixed;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 50;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.75rem 1.25rem;
    background: var(--bg);
    border-top: 1px solid var(--border);
    box-shadow: 0 -4px 16px rgba(0, 0, 0, 0.06);
  }
  /* Padding inferior para que la barra no tape contenido */
  .con-barra-movil {
    padding-bottom: 5.5rem;
  }
}
```

- [ ] **Step 2: Verificar typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: sin errores (CSS no afecta tipos, pero confirma que nada se rompió).

- [ ] **Step 3: Commit**

```bash
git add src/app/globals.css
git commit -m "feat(mobile): clases de layout responsivas + breakpoint 640px"
```

---

## Task 2: Panel del dashboard apila en móvil

**Files:**
- Modify: `src/app/dashboard/page.tsx`

- [ ] **Step 1: Reemplazar el grid inline por la clase `.grid-panel`**

Buscar:

```tsx
      <div
        style={{
          display: "grid",
          gridTemplateColumns: "minmax(280px, 360px) 1fr",
          gap: "1.5rem",
          alignItems: "start",
        }}
      >
```

Reemplazar por:

```tsx
      <div className="grid-panel">
```

(El `</div>` de cierre correspondiente no cambia.)

- [ ] **Step 2: Verificar typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add src/app/dashboard/page.tsx
git commit -m "feat(mobile): panel apila en una columna"
```

---

## Task 3: "Arma tu mesa" apila + barra de resumen móvil

**Files:**
- Modify: `src/app/dashboard/mesa/[slug]/page.tsx`

- [ ] **Step 1: Reemplazar el grid inline por `.grid-mesa`**

Buscar:

```tsx
      <div
        style={{
          display: "grid",
          gridTemplateColumns: "1fr minmax(280px, 340px)",
          gap: "1.75rem",
          alignItems: "start",
        }}
      >
```

Reemplazar por:

```tsx
      <div className="grid-mesa">
```

- [ ] **Step 2: Reemplazar el aside sticky inline por `.mesa-aside`**

Buscar:

```tsx
        <aside style={{ position: "sticky", top: "1.5rem" }}>
```

Reemplazar por:

```tsx
        <aside className="mesa-aside">
```

- [ ] **Step 3: Añadir clase de padding al `<main>` y la barra inferior móvil**

Buscar la apertura del main:

```tsx
    <main className="contenedor" style={{ paddingTop: "2rem", paddingBottom: "4rem", maxWidth: 1100 }}>
```

Reemplazar por (agrega `con-barra-movil` cuando hay regalos, para reservar espacio):

```tsx
    <main
      className={`contenedor${lista.length > 0 ? " con-barra-movil" : ""}`}
      style={{ paddingTop: "2rem", paddingBottom: "4rem", maxWidth: 1100 }}
    >
```

Luego, justo ANTES de la etiqueta de cierre `</main>` (al final del componente), insertar la barra fija (solo visible en móvil por CSS):

```tsx
      {lista.length > 0 && (
        <div className="barra-movil">
          <div>
            <div className="muted" style={{ fontSize: "0.75rem" }}>
              Total · {lista.length} regalos
            </div>
            <strong style={{ fontSize: "1.1rem" }}>{pesos(total)}</strong>
          </div>
          <Link href={`/dashboard/mesa/${slug}/resumen`} className="btn btn-primario">
            Continuar →
          </Link>
        </div>
      )}
```

`pesos`, `total`, `lista` y `slug` ya existen en este componente (`pesos` es la función local de formato; `total` es la suma; `slug` viene de los params). `Link` ya está importado.

- [ ] **Step 4: Verificar typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: sin errores.

- [ ] **Step 5: Commit**

```bash
git add src/app/dashboard/mesa/[slug]/page.tsx
git commit -m "feat(mobile): arma tu mesa apila + barra de resumen fija inferior"
```

---

## Task 4: Filas del catálogo se reacomodan en móvil

**Files:**
- Modify: `src/app/dashboard/mesa/[slug]/CatalogoSelector.tsx`

- [ ] **Step 1: Usar `.fila-catalogo` en el contenedor de cada ítem**

Buscar:

```tsx
            <div
              key={item.id}
              className="tarjeta"
              style={{
                display: "flex",
                alignItems: "center",
                gap: "1rem",
                padding: "0.6rem 0.85rem",
              }}
            >
```

Reemplazar por:

```tsx
            <div key={item.id} className="tarjeta fila-catalogo">
```

- [ ] **Step 2: Usar `.fila-catalogo-form` en el formulario de cada ítem**

Buscar:

```tsx
              <form
                action={agregarDesdeCatalogo.bind(null, slug, item.id)}
                style={{ flexShrink: 0, display: "flex", alignItems: "center", gap: "0.6rem" }}
              >
```

Reemplazar por:

```tsx
              <form
                action={agregarDesdeCatalogo.bind(null, slug, item.id)}
                className="fila-catalogo-form"
              >
```

- [ ] **Step 3: Verificar typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/app/dashboard/mesa/[slug]/CatalogoSelector.tsx
git commit -m "feat(mobile): filas del catálogo se reacomodan vertical en móvil"
```

---

## Task 5: Hero de la home con tipografía fluida

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Cambiar el `font-size` fijo del título por `clamp()`**

Buscar:

```tsx
        <h1 style={{ fontSize: "3.25rem", marginTop: "1.25rem" }}>
```

Reemplazar por:

```tsx
        <h1 style={{ fontSize: "clamp(2rem, 8vw, 3.25rem)", marginTop: "1.25rem" }}>
```

- [ ] **Step 2: Reducir el padding superior del hero en móvil sin perder el desktop**

Buscar:

```tsx
    <main className="contenedor" style={{ paddingTop: "5rem", paddingBottom: "5rem" }}>
```

Reemplazar por (padding superior fluido):

```tsx
    <main className="contenedor" style={{ paddingTop: "clamp(2.5rem, 8vw, 5rem)", paddingBottom: "5rem" }}>
```

- [ ] **Step 3: Verificar typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat(mobile): hero con tipografía y espaciado fluido"
```

---

## Task 6: Harness de verificación (usuario de prueba + Playwright)

**Files:**
- Create: `scripts/seed-usuario-prueba.mjs`
- Create: `scripts/capturas-movil.mjs`

> Estos scripts son solo para verificación local; leen credenciales de `.env.local`. No se importan desde la app.

- [ ] **Step 1: Instalar Playwright (devDependency) + Chromium**

Run:
```bash
pnpm add -D playwright
pnpm exec playwright install chromium
```
Expected: instala sin error.

- [ ] **Step 2: Crear `scripts/seed-usuario-prueba.mjs`**

```js
// Crea (idempotente) un usuario de prueba confirmado y le siembra una mesa con
// algunos regalos, para poder capturar las pantallas con sesión.
import { readFileSync } from "node:fs";
import { createClient } from "@supabase/supabase-js";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n")
    .filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => {
      const i = l.indexOf("=");
      return [l.slice(0, i).trim(), l.slice(i + 1).trim()];
    }),
);

const url = env.NEXT_PUBLIC_SUPABASE_URL;
const serviceKey = env.SUPABASE_SERVICE_ROLE_KEY;
const EMAIL = "mobiletest@regalove.test";
const PASSWORD = "Mobile-Test-1234";

const admin = createClient(url, serviceKey, { auth: { autoRefreshToken: false, persistSession: false } });

// 1) Usuario confirmado (si ya existe, lo reusa)
let userId;
const creado = await admin.auth.admin.createUser({
  email: EMAIL,
  password: PASSWORD,
  email_confirm: true,
});
if (creado.error && !creado.error.message.toLowerCase().includes("already")) {
  throw creado.error;
}
if (creado.data?.user) {
  userId = creado.data.user.id;
} else {
  const { data } = await admin.auth.admin.listUsers();
  userId = data.users.find((u) => u.email === EMAIL)?.id;
}
if (!userId) throw new Error("No se pudo obtener el id del usuario de prueba");

// 2) Mesa de prueba con ítems (idempotente por slug)
const slug = "mesa-de-prueba-movil";
const { data: existente } = await admin.from("eventos").select("id").eq("slug", slug).maybeSingle();
let eventoId = existente?.id;
if (!eventoId) {
  const { data, error } = await admin
    .from("eventos")
    .insert({ festejado_id: userId, tipo: "boda", titulo: "Mesa de prueba móvil", slug, codigo: "9001" })
    .select("id")
    .single();
  if (error) throw error;
  eventoId = data.id;

  const { data: cat } = await admin
    .from("catalogo_items")
    .select("id, nombre, descripcion, imagen_url, precio_centavos")
    .limit(4);
  await admin.from("items_mesa").insert(
    (cat ?? []).map((c, i) => ({
      evento_id: eventoId,
      catalogo_item_id: c.id,
      nombre: c.nombre,
      descripcion: c.descripcion,
      imagen_url: c.imagen_url,
      monto_meta_centavos: c.precio_centavos,
      cantidad: 1,
      orden: i,
    })),
  );
}

console.log(JSON.stringify({ EMAIL, PASSWORD, slug }));
```

- [ ] **Step 3: Crear `scripts/capturas-movil.mjs`**

```js
// Login a 390px y captura las pantallas clave. Uso:
//   node scripts/capturas-movil.mjs before
//   node scripts/capturas-movil.mjs after
import { chromium, devices } from "playwright";

const BASE = "http://localhost:3000";
const EMAIL = "mobiletest@regalove.test";
const PASSWORD = "Mobile-Test-1234";
const SLUG = "mesa-de-prueba-movil";
const TAG = process.argv[2] || "after";

const rutasPublicas = [
  ["home", "/"],
  ["login", "/login"],
  ["registro", "/registro"],
  ["regalar", "/regalar"],
  ["mesa-publica", `/${SLUG}`],
];
const rutasPrivadas = [
  ["dashboard", "/dashboard"],
  ["crear", "/dashboard/crear"],
  ["crear-rapido", "/dashboard/crear/rapido"],
  ["arma-mesa", `/dashboard/mesa/${SLUG}`],
  ["resumen", `/dashboard/mesa/${SLUG}/resumen`],
  ["recibido", `/dashboard/mesa/${SLUG}/recibido`],
];

const browser = await chromium.launch();
const ctx = await browser.newContext({ ...devices["iPhone 13"] });
const page = await ctx.newPage();

async function shot(nombre, ruta) {
  await page.goto(`${BASE}${ruta}`, { waitUntil: "networkidle" });
  await page.screenshot({ path: `/tmp/movil-${TAG}-${nombre}.png`, fullPage: true });
  console.log(`✓ ${nombre}`);
}

for (const [n, r] of rutasPublicas) await shot(n, r);

// Login
await page.goto(`${BASE}/login`, { waitUntil: "networkidle" });
await page.fill("#correo", EMAIL);
await page.fill("#password", PASSWORD);
await page.click('button[type="submit"]');
await page.waitForURL("**/dashboard**", { timeout: 15000 });

for (const [n, r] of rutasPrivadas) await shot(n, r);

await browser.close();
console.log("listo");
```

- [ ] **Step 4: Capturar baseline ANTES de los cambios visuales**

> Si ejecutas este plan en orden, las Tareas 1–5 ya cambiaron el layout. Para un "antes" real, captura ANTES contra `main`: en otra terminal `git stash` no aplica (ya commiteado). Alternativa: corre el baseline desde `main` antes de empezar las tareas. Si ya empezaste, omite el "before" y captura solo "after" (Step 6) — el objetivo principal es validar que el "after" se ve bien.

Run (con el dev server corriendo y la mesa sembrada):
```bash
node scripts/seed-usuario-prueba.mjs
node scripts/capturas-movil.mjs after
```
Expected: genera `/tmp/movil-after-*.png` para las 11 rutas sin error de login.

- [ ] **Step 5: Commit de los scripts**

```bash
git add scripts/seed-usuario-prueba.mjs scripts/capturas-movil.mjs package.json pnpm-lock.yaml
git commit -m "chore(mobile): scripts de verificación (usuario de prueba + capturas Playwright)"
```

---

## Task 7: Revisión visual

**Files:** ninguno.

- [ ] **Step 1: Abrir cada captura `/tmp/movil-after-*.png` y revisar**

Checklist por pantalla (390px):
- Nada se desborda horizontalmente (sin scroll lateral).
- **Home:** el título no se parte feo ni se sale.
- **Panel:** "Crear una mesa" y "Tus mesas" apilados; botones de cada mesa caben.
- **Arma tu mesa:** catálogo a todo el ancho, "En tu mesa" abajo, y **barra fija inferior** con total + "Continuar" visible.
- **Catálogo:** cada fila legible; precio/cantidad/Agregar en su propia línea cómoda.
- **Crear rápido:** slider y tarjeta del paquete caben bien.
- Login/registro/regalar/aportar/resumen/recibido: formularios y tarjetas centrados, sin desbordes.

- [ ] **Step 2: Arreglar lo que se vea mal**

Si algo se ve mal, ajustar el `globals.css` (breakpoint/clases) o el inline puntual, recommit, y re-correr `node scripts/capturas-movil.mjs after`.

---

## Self-review (cobertura del diseño)

- Estrategia CSS con breakpoint 640px → Task 1. ✅
- Panel apila → Task 2. ✅
- Arma tu mesa apila + barra fija inferior (carrito móvil) → Task 3. ✅
- Filas del catálogo se reacomodan → Task 4. ✅
- Tipografía fluida del hero → Task 5. ✅
- Tap targets ≥ cómodos (`.btn` padding en móvil) → Task 1. ✅
- Verificación con Playwright a 390px + usuario de prueba → Tasks 6–7. ✅
- Resto de pantallas (login/registro/regalar/aportar/resumen/recibido/retirar/personalizar): ya usan `maxWidth`+`auto-fit`; se validan visualmente en Task 7 y se ajustan solo si hace falta. ✅
- Fuera de alcance (rediseño visual, hamburguesa, cambios de lógica) → no se tocan. ✅
```
