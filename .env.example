# Marca / configuración base (editable sin tocar código)
NOMBRE_MARCA=RegalaLove
URL_BASE=http://localhost:3000

# Supabase — del proyecto dedicado a RegalaLove
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Secreto: solo lado servidor, NUNCA exponer al cliente ni commitear
SUPABASE_SERVICE_ROLE_KEY=

# Correo transaccional (Resend) — secretos solo de servidor
RESEND_API_KEY=
CORREO_REMITENTE=RegalaLove <no-responder@regalalove.com>

# EcartPay (pagos) — sandbox; pendiente de conectar con credenciales reales
ECARTPAY_API_KEY=
ECARTPAY_WEBHOOK_SECRET=
ECARTPAY_BASE_URL=

# Correos de administradores (separados por comas). Quien inicie sesión con un
# correo de esta lista ve el panel interno en /admin. Sin valor ⇒ sin acceso admin.
ADMIN_EMAILS=
